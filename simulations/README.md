# Simulations

`simulations/` is the parent folder for all simulation runs.

Current run folder:
- `test/`
- `deadband2/`

Future simulations should be added as new sibling subfolders under `simulations/`.
